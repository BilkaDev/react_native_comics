import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'column',
    alignItems: 'center',
    marginBottom: 10
  },
  title: {
    fontSize: 18,
    textAlign: 'center'
  },
  image: {
    width: '100%',
    aspectRatio: 1
  }
})
